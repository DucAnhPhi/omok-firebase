import { createProfileFunc } from "./profile";
import { createGameFunc, matchGameFunc, makeMoveFunc } from "./game";

export const createProfile = createProfileFunc;
export const createGame = createGameFunc;
export const matchGame = matchGameFunc;
export const makeMove = makeMoveFunc;
